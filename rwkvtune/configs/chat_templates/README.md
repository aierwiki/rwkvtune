# Chat Templates for RWKV7

This directory contains chat template files for training and inference.

## Template Format

Uses Jinja2 template syntax, compatible with Hugging Face chat_template.

## Default Template

**`rwkv_default.jinja`** - RWKV community standard format:

```
User: [user message]
Assistant: [assistant message]
```

